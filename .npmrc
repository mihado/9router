# Hardened fork: use a flat (hoisted) node_modules layout instead of pnpm's
# default symlinked store. The Dockerfile copies individual packages
# (node-forge, next) out of node_modules into the standalone runtime image;
# a hoisted layout keeps those as real directories so the COPY lines work.
node-linker=hoisted
